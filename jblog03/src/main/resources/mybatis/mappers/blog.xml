<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">
	<!--블로그 생성 -->
	<insert id="insert" parameterType="blogvo">
		<![CDATA[
		insert
		  into blog
	    values (#{title}, #{image}, #{blogId})
		]]>
	</insert>
	
	<!-- 블로그 정보 가져오기 -->
	<select id="blogInfo" resultType="blogvo" parameterType="map">
		<![CDATA[
		select title, image, blog_id
		  from blog
		 where blog_id=#{blogId }
		]]>
	</select>
	
	<!--블로그 생성 -->
	<insert id="insertCategory" parameterType="categoryvo">
		<![CDATA[
		insert
		  into category
	    values (null, #{name}, #{description}, #{blogId})
		]]>
	</insert>
	
		<!-- 카테고리 정보 가져오기 -->
	<select id="categoryInfo" resultType="categoryvo" parameterType="blogvo">
		<![CDATA[
		select no, name, description
		  from category
		 where blog_id=#{blogId }
		]]>
	</select>
	
</mapper>